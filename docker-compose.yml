# ══════════════════════════════════════════════════════════════════════════════
# goszakup-agent · docker-compose.yml · Spiral 1
#
# Spiral 1: postgres + adminer (DB UI) + redis
# Spiral 2 will add: etl_service, analytics_api
# Spiral 3 will add: milvus, agent_api
# Spiral 4 will add: nginx, airflow, prometheus, grafana
#
# Usage:
#   cp .env.example .env && nano .env   # fill secrets
#   docker compose up -d
#   docker compose logs -f postgres     # watch init
# ══════════════════════════════════════════════════════════════════════════════

name: goszakup-agent

# ── Networks (isolated zones) ──────────────────────────────────────────────────
networks:
  data_net:       # postgres, redis, milvus — NEVER exposed externally
    driver: bridge
    internal: true   # no internet access from this network
  app_net:        # services that talk to data_net AND app logic
    driver: bridge

# ── Named volumes (data survives container restarts) ──────────────────────────
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

services:

  # ── PostgreSQL 16 ────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: goszakup_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB:       ${POSTGRES_DB:-goszakup}
      POSTGRES_USER:     ${POSTGRES_USER:-goszakup_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - data_net
      - app_net
    ports:
      - "127.0.0.1:5432:5432"   # localhost ONLY — for host-run ETL/scripts
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-goszakup_user} -d ${POSTGRES_DB:-goszakup}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    # To access locally for debugging:
    #   psql "postgresql://goszakup_user:${POSTGRES_PASSWORD}@127.0.0.1:5432/${POSTGRES_DB:-goszakup}"

  # ── Redis (response cache, task queue later) ──────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: goszakup_redis
    restart: unless-stopped
    command: >
      redis-server
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
      --requirepass ${REDIS_PASSWORD:-redis_change_me}
    volumes:
      - redis_data:/data
    networks:
      - data_net
      - app_net
    ports:
      - "127.0.0.1:6379:6379"   # localhost ONLY — for host-run ETL/scripts
    healthcheck:
      test: ["CMD", "redis-cli", "--no-auth-warning", "-a", "${REDIS_PASSWORD:-redis_change_me}", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # ── Adminer (DB UI — development only) ───────────────────────────────────────
  adminer:
    image: adminer:4.8.1
    container_name: goszakup_adminer
    restart: unless-stopped
    environment:
      ADMINER_DEFAULT_SERVER: postgres
      ADMINER_DESIGN: pepa-linha-dark
    networks:
      - data_net
      - app_net
    ports:
      - "127.0.0.1:8080:8080"   # localhost ONLY — not 0.0.0.0
    depends_on:
      postgres:
        condition: service_healthy
    profiles:
      - dev    # only starts with: docker compose --profile dev up

  # ── ETL service (Spiral 2) ────────────────────────────────────────────────────
  # etl:
  #   build: ./etl
  #   ...

  # ── Analytics API (Spiral 2) ──────────────────────────────────────────────────
  analytics_api:
    build:
      context: .
      dockerfile: services/analytics_api/Dockerfile
    container_name: goszakup_analytics_api
    restart: unless-stopped
    environment:
      ANALYTICS_DB_PATH: /app/data/analytics.duckdb
      ANALYTICS_PARQUET_DIR: /app/data/parquet
    volumes:
      - ./:/app
    networks:
      - data_net
      - app_net
    ports:
      - "127.0.0.1:8001:8001"
    depends_on:
      postgres:
        condition: service_healthy

  # ── Milvus (Spiral 3) ─────────────────────────────────────────────────────────
  # milvus:
  #   image: milvusdb/milvus:v2.4.0
  #   ...

  # ── AI Agent API (Spiral 3) ───────────────────────────────────────────────────
  agent_api:
    build:
      context: .
      dockerfile: services/agent_api/Dockerfile
    container_name: goszakup_agent_api
    restart: unless-stopped
    environment:
      ANALYTICS_DB_PATH: /app/data/analytics.duckdb
    volumes:
      - ./:/app
    networks:
      - data_net
      - app_net
    ports:
      - "127.0.0.1:8002:8002"
    depends_on:
      analytics_api:
        condition: service_started

  # ── Demo UI (Spiral 4) ───────────────────────────────────────────────────────
  ui:
    build:
      context: .
      dockerfile: services/ui/Dockerfile
    container_name: goszakup_ui
    restart: unless-stopped
    environment:
      AGENT_API_URL: http://agent_api:8002
      ANALYTICS_API_URL: http://analytics_api:8001
    volumes:
      - ./:/app
    networks:
      - app_net
    ports:
      - "127.0.0.1:8501:8501"
    depends_on:
      agent_api:
        condition: service_started
